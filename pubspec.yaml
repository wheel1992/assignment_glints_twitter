name: assignment_glints_twitter
description: Assignment Glints Twitter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Firebase
  firebase_core: ^1.3.0
  firebase_auth: ^1.4.1
  cloud_firestore: ^2.2.2
  
  # GetX
  get: ^4.1.4
  
  # Misc
  json_annotation: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.4
  json_serializable: ^4.1.3

  # Testing
  fake_cloud_firestore: ^1.1.2
  firebase_auth_mocks: ^0.7.1
  mockito: ^5.0.10

flutter:
  uses-material-design: true